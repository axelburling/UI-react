import styled from 'styled-components';
import { headline4CSS } from '../../Styles';

const PageHeader = styled.h1`
  ${headline4CSS}
  font-weight: bold;
  margin-bottom: 0.75rem;
  hyphens: auto;
  word-break: break-word;
`;

export default PageHeader;
